name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Generate Prisma client
        run: bun --cwd=src/web run db:generate

      - name: Typecheck web
        # tsc OOMs on GitHub's standard 7GB runner against Prisma's generated
        # types at the default V8 heap limit; raise it explicitly.
        run: bun --cwd=src/web run typecheck
        env:
          NODE_OPTIONS: --max-old-space-size=6144

      - name: Typecheck terminal host
        run: bun --cwd=src/terminal-node run typecheck

      - name: Lint web
        run: bun --cwd=src/web run lint

      - name: Test web
        run: cd src/web && bun test
